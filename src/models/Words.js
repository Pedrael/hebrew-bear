class Word {
    constructor(root, translate, type)
    {
        this.root = root;
        this.translate = translate;
        this.type = type;
    }

    getUser() {
        return this;
    }
}

export default Word;